;;; Use Karabiner (https://github.com/tekezo/Karabiner-Elements) to
;;; create the following layout of modifier keys:
;;;
;;;     fn command option ctrl space ctrl option command (fn)

(setq mac-control-modifier 'control)
(setq mac-option-modifier 'meta)
(setq mac-command-modifier 'super)
(setq ns-function-modifier 'hyper)

(provide 'setup-mac-os)
